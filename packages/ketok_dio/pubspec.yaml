name: ketok_dio
description: Dio interceptor for Ketok, the in-app Flutter network inspector. Captures requests, responses, and errors, and supports one-tap replay.
version: 0.1.0
repository: https://github.com/setilanaji/ketok

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  dio: ^5.9.0
  ketok_core: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
