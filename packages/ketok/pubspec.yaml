name: ketok
description: >
  In-app Flutter network inspector. Facade over ketok_core + ketok_ui with
  zero-config initialize, production-safe defaults, and a draggable overlay.
version: 0.1.0
repository: https://github.com/setilanaji/ketok

environment:
  sdk: ^3.11.0
  flutter: ">=3.35.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  ketok_core: ^0.1.0
  ketok_ui: ^0.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
