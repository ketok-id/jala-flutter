name: jala_graphql
description: >
  GraphQL link for Jala: operation-aware capture and subscription payload
  timelines for gql_link clients (graphql_flutter, ferry).
version: 0.5.2
homepage: https://github.com/ketok-id/jala-flutter
repository: https://github.com/ketok-id/jala-flutter
issue_tracker: https://github.com/ketok-id/jala-flutter/issues
topics:
  - network
  - debugging
  - devtools
  - graphql

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  gql: ^1.0.0
  gql_exec: ^1.1.0
  gql_link: ^1.0.0
  jala_core: ^0.5.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
