name: jala_core
description: >
  Pure-Dart core for Jala: call model, store, redaction, filter grammar,
  exporters, call/JSON diff, cURL/HAR import, network throttling, and
  session export/import. Zero Flutter.
version: 0.6.0
homepage: https://github.com/ketok-id/jala-flutter
repository: https://github.com/ketok-id/jala-flutter
issue_tracker: https://github.com/ketok-id/jala-flutter/issues
topics: [network, debugging, devtools, http]
resolution: workspace

environment:
  sdk: ^3.11.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
