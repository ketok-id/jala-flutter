name: jala_ui
description: Inspector UI for Jala, the in-app Flutter network inspector. Call list with DevTools-style filtering, detail tabs, JSON viewer, and overlay bubble.
version: 0.4.0
repository: https://github.com/ketok-id/jala-flutter
topics: [network, debugging, devtools, ui]

environment:
  sdk: ^3.11.0
  flutter: ">=3.35.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  jala_core: ^0.4.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
