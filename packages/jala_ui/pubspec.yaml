name: jala_ui
description: >
  Inspector UI for Jala: filtered call list, detail tabs, throttle screen,
  session export/import, JSON viewer, and overlay bubble.
version: 0.5.2
homepage: https://github.com/ketok-id/jala-flutter
repository: https://github.com/ketok-id/jala-flutter
issue_tracker: https://github.com/ketok-id/jala-flutter/issues
topics: [network, debugging, devtools, ui]

environment:
  sdk: ^3.11.0
  flutter: ">=3.35.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  jala_core: ^0.5.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
