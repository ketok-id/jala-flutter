name: jala_dio
description: >
  Dio interceptor for Jala: capture, one-tap replay, and network throttling
  (latency, drop, stream bandwidth pacing).
version: 0.5.2
homepage: https://github.com/ketok-id/jala-flutter
repository: https://github.com/ketok-id/jala-flutter
issue_tracker: https://github.com/ketok-id/jala-flutter/issues
topics: [network, debugging, devtools, http, dio]

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  dio: ^5.9.0
  jala_core: ^0.5.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
